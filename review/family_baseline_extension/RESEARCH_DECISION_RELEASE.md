# Baseline and reversal release decision

H improves over F_B in ORIGINAL on log loss and Brier at all three frozen penalties; random-orientation results vary. This is exploratory known-family held-controller prediction, not demonstrated crossed specialization or population specificity.

The reversal diagnostic found no qualifying training pair among 66 candidates under fixed 30/30 halves and ±0.05 margins. Held evaluations: zero; held outcomes were not parsed. This no-go does not prove absence of localized or smaller specialization. Reversal checks were same-executor separate arithmetic, not an independent reviewer audit.

The F_B independent numerical audit passed. Its results file retains the historical pending-audit field; FAMILY_BASELINE_AUDIT.json and the closeout give the subsequent disposition. The initial schedule-format failure is retained in FAMILY_BASELINE_RESULTS_INITIAL_INCOMPLETE.json; a separately frozen padded adapter resolved it without retuning. Prior geometry-specificity numerical failure and inconclusive classification remain unchanged. Record-loss cause remains unestablished. Unknown-attempt ledger markers are not scientific observations.

Original stage documents retain their historical no-publication and next-step language. This later release does not authorize those steps. Supervisor owns the next research decision; writing remains user-owned. No new scientific calculation or private-data access was performed for release.

Public code is limited to pure numerical libraries and synthetic tests. Run `python -m pytest -q -p no:cacheprovider review/family_baseline_extension/test_family_baseline.py review/reversal_diagnostic/test_reversal.py` with NumPy and pytest; baseline tests also use the already published model_comparison/finite_models.py. Private reconstruction adapters and operational prechecks are omitted; the public bundle does not reproduce private-data execution end to end. Opaque private-input hashes authenticate recorded provenance without exposing their contents. Controller-wise summaries aggregate families; no per-family arrays, raw scores, identities or vectors are included.
