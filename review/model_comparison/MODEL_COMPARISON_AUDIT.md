# Independent retrospective model-comparison audit

**PASS for the numerical and finite-execution audit; no passed full robust-benefit gate.** No fitting, inference, estimate changes, commit or push. Publication and the next program decision remain with the supervisor.

Separate NumPy code reconstructed the sealed outcome tensor and every saved fit's logits. Complex-step derivatives checked all 780 optima, including the normalized right factor and intrinsic tangent gradient. Maximum gradient per training trial was 9.9787377289e-10 against the unchanged 1e-9 criterion. Maximum objective difference was 1.14e-13; maximum held-out loss/paired-summary difference was 1.49e-13 (audit tolerances 1e-8 objective, 1e-10 summaries). All 510 selected fit groups, 114 loss rows, 90 paired summaries and 18 assignment controls agreed. Controller diagnostics and family-first aggregation agreed. All orientation contrasts are recorded in JSON.

The original Stage B G−H gain is .012270759 at lambda 1, .004478689 at .25 and .008919564 at 4. The .25 gain misses the exploratory .005 threshold. Original Stage A D−C at lambda 4 is −.000403524 in reverse: benefit is not stable across all penalties. Actual-coordinate H alone is insufficient as a control comparison: original actual-minus-permuted incremental G−H gains are .022525640, .012919110 and .019630138. Thus the controls affect global G as well as H. These conditional predictive results do not establish semantic specificity, population specificity or deployment usefulness. Greater latent flexibility and unequal signal-to-noise remain unresolved.

Low absolute losses are compatible with observed homogeneity: 42, 42 and 43 of 60 families have identical binary outcomes across all controllers and rollouts in the respective orientations. Per-trial normalization was independently checked. Stage A masks separate rollout halves; Stage B removes every observation of the held controller and uses only the eleven training coordinates for centering. Intercepts and penalties use only training sufficient statistics. No held labels select starts. Normalized factors have radial redundancy; complex-step differentiation includes normalization, and the gradient test restores the intrinsic scale. No global optimum is asserted.

The fixed plan has 780 checkpoints and exactly 1,560 alternating begin/end journal entries. All nine historical input hashes and all frozen code hashes agree. Checkpoint/journal hashes were unchanged across audit execution. Original losses and fitted parameters were never overwritten. The declared freeze and code/precheck filesystem timestamps precede the first checkpoint. Journal entries lack wall-clock timestamps, so chronology is supported by metadata rather than cryptographically proven. No semantic rescoring or historical recovery investigation was repeated.

Eighteen of 270 nonconvex groups have objective differences above 1e-6; the largest is 11.147836. Independent arithmetic turns one near tie into an exact tie. Original selection correctly follows the saved training objectives, and is retained; the audit records this arithmetic detail without relaxing tolerance or selecting by test loss.

Nine independent synthetic tests passed, covering all seven model derivatives, radial invariance, per-trial normalization and malformed/leaking masks. No primary model, prediction, loss or aggregation helper is imported. Shared inputs are the pinned historical scorer outputs, vectors, schedule and documented parameter layout; this is arithmetic independence, not an independent semantic scorer. The final remote audit took 8.33 CPU seconds and 8.34 wall seconds under a 30-minute cap. Two earlier read-only audit checks stopped during tie checking and output serialization; they added no fitting attempts. Historical fitting reported 8.77 CPU seconds and 19.71 wall seconds, within 30 minutes, although its frozen enforced ceiling was two hours. This distinction is preserved.

The JSON pins inputs, artifacts, the private checkpoint-manifest digest, individual checks, differences and limitations. Private scores, vectors, fit parameters and infrastructure paths remain at the execution site or in private supervision.
